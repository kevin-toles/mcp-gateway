# mcp-gateway tool registry configuration
# Defines all platform tools with metadata for MCP protocol registration.
# ai-agents A2A tools bridge MCP → A2A → Temporal for durable agent execution.
# Reference: Strategy §4.1, §6.3 (Dynamic Tool Listing), AC-8.4
tools:
  - name: semantic_search
    description: "Search across code, documentation, and textbooks using semantic similarity"
    when_to_use: "Low-level vector search. Use when you need exact collection targeting and threshold control with no fusion."
    tier: bronze
    tags: [search, rag]

  - name: hybrid_search
    description: "Combines semantic search with keyword matching for better precision"
    when_to_use: "Expert use only — use ask/search_in/find_code_pattern instead. Call directly only when you need to tune semantic_weight, bloom_tier_boost, or other routing internals."
    tier: bronze
    tags: [search, rag]

  - name: graph_traverse
    description: "Traverse the Neo4j knowledge graph via BFS or MMR diversity-aware traversal from one or more starting nodes"
    tier: silver
    tags: [graph, traversal, rag]

  # Issue #6: consolidated KB tools (knowledge_search / knowledge_refine / pattern_search)
  - name: knowledge_search
    description: "Batteries-included KB search across all collections (chapters, code, patterns) with taxonomy expansion ON by default. Use for broad knowledge retrieval."
    when_to_use: "Expert use only — exposes expand_taxonomy, mmr_rerank, bloom_tier_filter. Use ask facade instead for general questions."
    tier: bronze
    tags: [search, rag, knowledge]

  - name: knowledge_refine
    description: "Targeted single-collection KB search with optional Bloom or CRE tier filters. Use when you know which collection has the relevant content."
    when_to_use: "Expert use only — collection-scoped with mmr controls. Use search_in facade for standard shelf queries."
    tier: bronze
    tags: [search, rag, knowledge]

  - name: pattern_search
    description: "Code pattern and anti-pattern retrieval. Searches code_good_patterns, pattern_instances, or all collections. Use to find design patterns, code smells, or best-practice examples."
    when_to_use: "Use find_code_pattern facade for standard code example queries. Use pattern_search directly only when you need pattern_type control beyond good/bad/both."
    tier: bronze
    tags: [search, rag, patterns, code]

  - name: diagram_search
    description: "Semantic search over KB architecture diagrams (ASCII art, sequence diagrams, flow charts) using CLIP cross-modal encoding. Use to find visual representations of architectural concepts, design patterns, or system interactions."
    when_to_use: "Architecture diagram search. Use directly — no facade equivalent needed."
    tier: bronze
    tags: [search, rag, diagrams, architecture]

  - name: code_analyze
    description: "Analyze code for patterns, complexity, dependencies, and quality metrics"
    tier: silver
    tags: [code, analysis]

  - name: code_pattern_audit
    description: "Detect code anti-patterns using dual-net detection with confidence scoring"
    tier: silver
    tags: [code, audit]

  - name: graph_query
    description: "Query the Neo4j knowledge graph using Cypher (read-only)"
    tier: gold
    tags: [graph, query]

  - name: llm_complete
    description: "Generate LLM completion with tiered fallback (local → cloud)"
    tier: gold
    tags: [llm, completion]

  # ── A2A Protocol Tools (MCP ↔ A2A ↔ Temporal bridge) ──────────────

  - name: a2a_send_message
    description: "Send a message to an AI agent via A2A protocol. Creates a durable task routed through Temporal workflows when available."
    tier: gold
    tags: [a2a, agent, temporal]

  - name: a2a_get_task
    description: "Get the status and results of an A2A agent task by its ID"
    tier: bronze
    tags: [a2a, agent, status]

  - name: a2a_cancel_task
    description: "Cancel a running A2A agent task by its ID"
    tier: silver
    tags: [a2a, agent, cancel]

  # ── Workflow tools (WBS-WF6) ──────────────────────────────────────
  - name: convert_pdf
    description: "Convert a PDF file to structured JSON. Extracts text per page with optional OCR fallback and detects chapters. Requires code-orchestrator (:8083)."
    tier: gold
    tags: [workflow, pdf, conversion]

  - name: extract_book_metadata
    description: "Extract metadata, keywords, concepts, and code blocks from a book JSON file. Requires code-orchestrator (:8083)."
    tier: gold
    tags: [workflow, extraction, metadata]

  - name: batch_extract_metadata
    description: "Extract metadata from all books in a directory with live progress reporting. Processes each book sequentially through CO, reporting book-by-book progress. Requires code-orchestrator (:8083)."
    tier: gold
    tags: [workflow, extraction, metadata, batch]

  - name: generate_taxonomy
    description: "Build full taxonomy from enriched corpus (*_enriched.json files). Aggregates keywords/concepts per book, applies quality gates, infers Bloom tiers, and writes uber_taxonomy format (metadata + concept_categories + tiers). Pass enriched_dir and optional output_path. Requires code-orchestrator (:8083)."
    tier: gold
    tags: [workflow, taxonomy, concepts, enriched]

  - name: enrich_book_metadata
    description: "Enrich a single book's extracted metadata to DMA-§2.3 format via CO pipeline (keywords/concepts/classify/similarity/topics). No ai-agents dependency. Requires code-orchestrator (:8083)."
    tier: gold
    tags: [workflow, enrichment, dma, direct_co]

  - name: batch_enrich_metadata
    description: "Batch-enrich all *_metadata.json files in a directory to DMA-§2.3 format. Accepts metadata_dir, output_dir, taxonomy_path, resume, limit, book filter. Requires code-orchestrator (:8083)."
    tier: gold
    tags: [workflow, enrichment, batch, dma, direct_co]

  - name: enhance_guideline
    description: "Enhance a guideline's chapters via LLM with cross-book context. Requires ai-agents (:8082)."
    tier: gold
    tags: [workflow, enhancement, llm]

  # ── Taxonomy Analysis (WBS-TAP9) ─────────────────────────────────────
  - name: analyze_taxonomy_coverage
    description: "Analyze taxonomy coverage against the knowledge base. Runs full INGEST→GENERATE→SEARCH→SCORE→REPORT pipeline. Requires code-orchestrator (:8083)."
    tier: gold
    tags: [workflow, taxonomy, analysis, coverage]

  # ── AMVE Architecture Analysis (AEI-7) ────────────────────────────────
  - name: amve_detect_patterns
    description: "Detect architecture patterns in source code via AMVE. Requires AMVE service (:8088)."
    tier: gold
    tags: [amve, architecture, patterns, analysis]

  - name: amve_detect_boundaries
    description: "Detect service boundaries in source code via AMVE. Requires AMVE service (:8088)."
    tier: gold
    tags: [amve, architecture, boundaries, analysis]

  - name: amve_detect_communication
    description: "Detect event-driven and message bus patterns with scope routing via AMVE. Requires AMVE service (:8088)."
    tier: gold
    tags: [amve, architecture, communication, events, messaging]

  - name: amve_build_call_graph
    description: "Build call graph from source code via AMVE. Requires AMVE service (:8088)."
    tier: gold
    tags: [amve, architecture, call-graph, analysis]

  - name: amve_evaluate_fitness
    description: "Evaluate architecture fitness functions against a snapshot via AMVE. Requires AMVE service (:8088)."
    tier: gold
    tags: [amve, architecture, fitness, evaluation]

  - name: amve_generate_architecture_log
    description: "Generate architecture log via batch scan with optional baseline comparison via AMVE. Requires AMVE service (:8088)."
    tier: gold
    tags: [amve, architecture, batch-scan, baseline]

  # AEI-17: Dead Code Detection
  - name: amve_detect_dead_code
    description: "Detect dead code (unused functions + unused imports) in a source tree via AMVE. Requires AMVE service (:8088)."
    tier: gold
    tags: [amve, architecture, dead-code, unused-imports]

  # Phase 2: Content-Addressed Snapshot Store (flag: SNAPSHOT_STORE_ENABLED)
  - name: amve_extract_architecture
    description: "Extract architecture snapshot from source code and compute a content-addressed SHA-256 fingerprint. Returns snapshot_sha, record_count, source_path, extraction_time_ms. Requires AMVE service (:8088) and SNAPSHOT_STORE_ENABLED=true."
    tier: gold
    tags: [amve, architecture, snapshot, sha, content-addressed]

  - name: amve_detect_drift
    description: "Detect architectural drift between two snapshots. Accepts SHA-pair mode (snapshot_a_sha + snapshot_b_sha retrieved from Redis stream) or raw-dict passthrough mode (snapshot_a + snapshot_b dicts). Requires AMVE service (:8088)."
    tier: gold
    tags: [amve, architecture, drift, snapshot, comparison]

  # ── Audit Service Tools (WBS-AEI13) ─────────────────────────────────────
  - name: audit_security_scan
    description: "Scan source code for security vulnerabilities using regex, AST, and semantic detection. Requires audit-service (:8084)."
    tier: silver
    tags: [audit, security, code]

  - name: audit_code_metrics
    description: "Compute structural, architectural, and eloquence pillar scores for source code. Requires audit-service (:8084)."
    tier: silver
    tags: [audit, metrics, code]

  - name: audit_corpus_search
    description: "Search the code corpus and textbook chapters using Qdrant vector similarity. Requires audit-service (:8084)."
    tier: gold
    tags: [audit, search, rag]


  - name: audit_dependency_assess
    description: >
      Analyse declared dependencies for health violations including DEP_DEAD,
      DEP_HEAVY, DEP_PASSTHROUGH, DEP_LOW_RATIO, DEP_UNSTABLE, and
      DEP_FACADE_CANDIDATE. Returns dependency_health_score in [0,100],
      zone classification (healthy/tolerable/questionable/bloated), and
      Martin metrics per package. Requires audit-service (:8084).
    tier: bronze
    tags: [audit, dependency, metrics]

  - name: audit_resolve_lookup
    description: >
      Look up the full resolution evidence chain for a violation type.
      Searches the resolution knowledge base (Qdrant :6335) and returns:
      the matched violation type, the governing principle, the resolution
      pattern (technique + effort estimate), documentation references
      (source book + chapter), and optional code examples from the corpus.
      Works across all 5 pillars: structural, architectural, eloquence,
      security, dependency. Requires audit-service (:8084) and
      qdrant-resolution (:6335).
    tier: gold
    tags: [audit, resolve, compliance, evidence-chain]


  # AEI-23: VRE Quarantine Infrastructure
  - name: audit_search_exploits
    description: >
      Search the vulnerability quarantine knowledge base (Qdrant :6336) for
      exploit vectors matching a natural-language query. Uses CodeBERT
      (768-dim) embeddings via code-orchestrator (:8083) to embed the query
      and performs vector similarity search on the vuln_exploits collection.
      Optional CWE ID filter restricts results to specific vulnerability
      classes. Returns ranked ExploitMatch objects with CVE ID, CWE IDs,
      CVSS score, severity, and exploit code. Requires audit-service (:8084)
      and qdrant-quarantine (:6336).
    tier: silver
    tags: [audit, security, exploit, cve, vre]

  - name: audit_search_cves
    description: >
      Query the CVE structured database (PostgreSQL :5432) for CVE records
      filtered by CWE identifier, severity level, and/or affected ecosystem.
      Returns structured CVERecord objects with CVSS scores, publication date,
      and reference URLs. All filters are optional — omitting them returns up
      to 'limit' records. Requires audit-service (:8084) and PostgreSQL (:5432).
    tier: silver
    tags: [audit, security, cve, cwe, vulnerability]

  - name: audit_quality_scan
    description: >
      Audit source code for coding pattern violations and structural
      anti-patterns. Runs CP001-CP011 (function design, naming, idioms),
      SA001-SA008 (test smells, dead abstractions, framework leakage), and
      SEC001-SEC008 (wildcard imports, unsafe yaml, disabled SSL, etc.) plus
      structural anti-pattern detection (Blob/God Class, Lava Flow, Boat Anchor,
      Redundant Wrapper, Premature Abstraction) using stdlib AST — no external
      dependencies required. Requires audit-service (:8084).
    tier: bronze
    tags: [audit, quality, patterns, antipatterns, static-analysis]

  # ── MCP Facade Tools (MCP-F) ─────────────────────────────────────────

  - name: ask
    description: >
      General software-engineering question answered from the KB.
      Use this as the default starting point — it automatically selects the
      right collections and query strategy. Supports optional difficulty hint
      ("beginner", "intermediate", "advanced") to constrain Bloom taxonomy tier.
    when_to_use: "Default. Start here for any general or cross-cutting question."
    tier: bronze
    tags: [search, rag, facade]

  - name: search_in
    description: >
      Targeted search within a specific knowledge shelf.
      Use when you know the content type: "textbooks" (concept explanations),
      "code" (implementation examples), "patterns" (design patterns / anti-patterns),
      or "diagrams" (architecture diagrams). More precise than ask for shelf-specific lookups.
    when_to_use: "Use when you know the type of content you need: textbooks, code, patterns, or diagrams."
    tier: bronze
    tags: [search, rag, facade]

  - name: find_code_pattern
    description: >
      Find code examples for a software practice.
      Use when you need concrete code, not conceptual explanation.
      examples="good" returns correct implementations; "bad" returns anti-patterns; "both" returns both.
    when_to_use: "Use when you need concrete code examples, not conceptual explanation."
    tier: bronze
    tags: [search, rag, facade, code, patterns]
